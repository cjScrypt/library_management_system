model Book {
    id                  Int     @id @default(autoincrement())
    isbn                String  @unique
    title               String
    author              String
    copiesAvailable     Int
    pages               Int
    records             Record[]
}